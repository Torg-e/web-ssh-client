# web-ssh-client
A modern-looking web SSH client focused on session recordings and built-in AI that compiles sessions into compact documentation. Includes a WireGuard client for accessing hosts in other networks without agent-based SSH.
